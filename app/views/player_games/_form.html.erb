<%= form_for(@player_game) do |f| %>
  <% if @player_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_game.errors.count, "error") %> prohibited this player_game from being saved:</h2>

      <ul>
      <% @player_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.text_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br />
    <%= f.text_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :cards_played %><br />
    <%= f.text_field :cards_played %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
